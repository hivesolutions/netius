{
    "version": "0.2.0",
    "configurations": [{
        "name": "Python",
        "type": "python",
        "request": "launch",
        "python": "${command:python.interpreterPath}",
        "program": "${file}",
        "debugOptions": [
            "RedirectOutput"
        ],
        "env": {
            "PYTHONPATH": "${workspaceRoot}/src:{env.PYTHONPATH}"
        }
    }]
}
