from netius.servers.asgi_neo import (
    ASGI_VERSION as ASGI_VERSION,
    ASGIServer as ASGIServer,
    BINARY_OPCODE as BINARY_OPCODE,
    BUFFER_SIZE as BUFFER_SIZE,
    CLOSE_CODE as CLOSE_CODE,
    CLOSE_NONE as CLOSE_NONE,
    CLOSE_OPCODE as CLOSE_OPCODE,
    CLOSE_REJECT as CLOSE_REJECT,
    CONTINUATION_OPCODE as CONTINUATION_OPCODE,
    COMPRESSED_LIMIT as COMPRESSED_LIMIT,
    LIFESPAN_INTERVAL as LIFESPAN_INTERVAL,
    LIFESPAN_TIMEOUT as LIFESPAN_TIMEOUT,
    LIFESPAN_VERSION as LIFESPAN_VERSION,
    MAX_PENDING as MAX_PENDING,
    PING_OPCODE as PING_OPCODE,
    PONG_OPCODE as PONG_OPCODE,
    PUMP_LIMIT as PUMP_LIMIT,
    SPEC_VERSION as SPEC_VERSION,
    TEXT_OPCODE as TEXT_OPCODE,
    hello_app as hello_app,
    load_app as load_app,
)

UNSUPPORTED_MESSAGE: str
